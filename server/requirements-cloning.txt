# File-ref voice-cloning server (tts_server.py + backends/qwen.py, mlx-audio backend).
# Pins lifted from the proven SanMarcSoft reference deploy (verified end-to-end).
# This is the server the Phenom dev-environment access layer (sablier-weblogon) drives;
# it is distinct from the CustomVoice server (server/requirements.txt).
mlx==0.30.4
mlx-metal==0.30.4
mlx-lm==0.30.5
mlx-audio==0.3.0
fastapi==0.128.0
uvicorn==0.40.0
soundfile==0.13.1
huggingface_hub==1.3.4
numpy==1.26.4
pydantic==2.12.5
